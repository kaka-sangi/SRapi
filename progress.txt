SRapi sub2api parity batch1 progress

Stage 1 complete - data layer foundation
- Added AccountGroup.rate_multiplier as an 8-place decimal string with default 1.00000000.
- Added UsageLog.actual_cost and UsageLog.rate_multiplier as 8-place decimal strings; migration backfills actual_cost from existing cost.
- Regenerated Ent code and refreshed Atlas migration hash.
- Synchronized account/usage contracts, memory stores, and Ent stores with backward-compatible defaults.

Evidence:
- make migration-check: ok github.com/srapi/srapi/apps/api/internal/platform/db 0.130s
- cd apps/api && go test ./internal/modules/accounts/... ./internal/modules/usage/... ./internal/persistence/entstore/accounts ./internal/persistence/entstore/usage ./internal/platform/db: all packages passed.

Next:
- Stage 2: pricing family fallback and rate_multiplier actual_cost billing logic with focused tests.

Stage 2 complete - pricing fallback and multiplier billing
- EstimatePrice now falls back to active PricingRule rows whose model family fuzzily matches the requested model family when no exact model rule exists.
- Ent subscription store enriches pricing rules with model family from model_registry for fallback matching.
- Gateway usage recording applies active account-group rate multipliers to actual_cost and stores the rate_multiplier snapshot on UsageLog while preserving standard cost.

Evidence:
- cd apps/api && go test ./internal/modules/subscriptions/service -run 'TestEstimatePrice(FallsBackToModelFamilyRule|UsesDecimalSafeProviderSpecificRulesAndOverrides)': ok.
- cd apps/api && go test ./internal/httpserver -run 'TestRecordGatewayUsageAppliesAccountGroupRateMultiplier': ok.

Next:
- Stage 3: separate synthetic quota snapshots from real provider quota signals for scheduler and alert reads.

Stage 3 complete - real quota signals separated from synthetic snapshots
- Synthetic usage-derived quota snapshots now use quota_type=synthetic_monthly_tokens.
- Added a migration that renames existing local synthetic monthly_tokens/unlimited rows to synthetic_monthly_tokens; no legacy runtime compatibility path remains.
- Account quota listing now applies limit per quota_type, so one synthetic or noisy quota type cannot crowd out other quota signals.
- Scheduler runtime state, admin health quota overlay, and quota-alert worker ignore synthetic quota snapshots and use only real upstream quota signals.

Evidence:
- cd apps/api && go test ./internal/httpserver -run 'Test(AccountSchedulerRuntimeStateIgnoresSyntheticQuotaSnapshots|RecordGatewayUsagePersistsProviderQuotaSignals)': ok.
- cd apps/api && go test ./internal/workers/account_quota_alert -run 'TestRunOnce(IgnoresSyntheticQuotaSnapshots|EnqueuesQuotaAlertOnRemainingRatioCrossing)': ok.
- cd apps/api && go test ./internal/persistence/entstore/accounts -run 'TestStoreListsLatestQuotaSnapshotPerType': ok.
- make migration-check: ok github.com/srapi/srapi/apps/api/internal/platform/db 0.131s.

Next:
- Stage 4: add GET /api/v1/me/platform-quotas via OpenAPI-first flow.

Stage 4 complete - current user platform quota endpoint
- Added GET /api/v1/me/platform-quotas to the OpenAPI contract and regenerated Go OpenAPI types, Copilot spec, and TypeScript SDK.
- Added a current-user handler that requires a console session and returns only quotas for the logged-in user.
- Added a focused HTTP test for anonymous 401 and current-user-only quota listing.

Evidence:
- cd apps/api && go test ./internal/httpserver -run 'TestCurrentUserPlatformQuotasListsOwnQuotas': ok.
- make openapi-lint: valid with the existing 3 public-endpoint 4xx warnings.
- make openapi-codegen-check: ok.
- make openapi-ts-codegen-check: ok.
- make sdk-ts-typecheck: ok.

Next:
- Stage 5: frontend IA regrouping, /admin/channels/[id] aggregate tabs, and dashboard balance/platform quota cards.

Stage 5 complete - frontend IA and user quota cards
- Regrouped admin nav: ADMIN_GATEWAY_SECTION is now 5 items, channelsPricing moved into Gateway, and channelsPricing is no longer in Commerce.
- Added /admin/channels/[id] as a provider/channel aggregate page with tabs for pricing, model mappings, model limits, payload rules, error passthrough, and TLS fingerprints.
- Wired GET /api/v1/me/platform-quotas into the TypeScript meApi/query layer.
- Added dashboard top cards for current balance and enabled platform quotas, above the usage KPI row.
- Fixed two React compiler/typecheck blockers found by the frontend quality gate: resource form inline label components and the invalid codex_cli PlatformFamily entry.

Evidence:
- cd apps/web && npm run lint: exit 0, with existing unused Skeleton warnings only.
- cd apps/web && npx tsc --noEmit: exit 0.
- cd apps/web && npm test -- --run tests/unit/messages.test.ts tests/unit/i18n-translate.test.ts: 5 tests passed.

Next:
- Stage 6: run broad backend/frontend gates, start local app, and capture browser evidence for channel tabs and dashboard balance card.

Stage 6 partial - broad quality gates
- Removed remaining seed-data assumptions in affected httpserver tests after the seed mock account removal: gateway/API-key limit tests, metrics, admin catalog overview/quota assertions, provider alias route tests, and provider preset install status expectations now create or expect explicit test-local state.
- Split token-count and Anthropic usage helpers out of oversized runtime/provider files while keeping behavior unchanged.
- Confirmed provider-scoped vision capability is part of gateway capability filtering.

Evidence:
- cd apps/api && go build ./...: exit 0.
- cd apps/api && go vet ./...: exit 0.
- cd apps/api && go test ./...: exit 0; internal/httpserver passed in 190.286s.
- cd apps/web && npm run lint: exit 0, with existing unused Skeleton warnings only.
- cd apps/web && npx tsc --noEmit: exit 0.

Next:
- Start the local app and capture browser evidence for /admin/channels/[id] tab switching and dashboard balance/platform quota cards.

SRapi sub2api parity batch2 progress

Stage 1 complete - shared money package
- Added apps/api/internal/pkg/money as the single decimal money helper package with DefaultCurrency, ZeroAmount, DecimalRat, RequiredDecimalRat, ParseMoneyRat, FormatRatFixed, AddMoney, NormalizeAmount, and NormalizeCurrency.
- Added money tests for blank/invalid parse handling, 8-place formatting, half-away rounding including negatives, invalid operand handling, and currency normalization.

Stage 2 complete - duplicate money helpers removed
- Replaced duplicated decimalRat/formatRatFixed/addMoney/parse helpers across usage, subscriptions, billing, httpserver gateway, balance_charger, affiliate, admin_control, users, and payments/provider code.
- Kept mandatory-input semantics through money.RequiredDecimalRat where old code rejected blanks.

Stage 3 complete - pricing moved to billing domain
- Moved PricingRule contract types and PricingStore to billing contract.
- Moved EstimatePrice, selectPricingRule, priceFromRule, cacheWriteRateOrInput, rule validation, and pricing CRUD orchestration to apps/api/internal/modules/billing/service/pricing.go.
- Billing memory and Ent stores now implement PricingStore; subscriptions store/service/contract no longer expose pricing APIs.
- HTTP admin pricing endpoints, config snapshot, audit mapping, and gateway pricing use runtime.billing.

Stage 4 complete - gateway billing orchestration
- Added billing Service.PriceGatewayUsage and PriceGatewayCost to centralize EstimatePrice, pricing source selection, account rate multiplier application, and subscription allowance billable-cost split.
- HTTP runtime now records cost/actual_cost/billable_cost from billing service output while still collecting subscription allowance inputs from the subscriptions service.

Evidence:
- cd apps/api && go build ./...: exit 0.
- cd apps/api && go vet ./...: exit 0.
- cd apps/api && go test ./...: exit 0; internal/httpserver passed in 226.093s.
- grep definitions: rg -n "func (addMoney|formatRatFixed|decimalRat)\b|type decimalRat|func parseMoneyRat" apps/api/internal apps/api/cmd returns only apps/api/internal/pkg/money/money.go.
- pricing location grep: selectPricingRule, priceFromRule, cacheWriteRateOrInput, PriceGatewayUsage, and PriceGatewayCost appear only under billing service plus HTTP call sites; subscriptions has no pricing API or implementation hits.

Next:
- Batch3: continue with the next goal document. Browser validation remains skipped per user instruction.

SRapi sub2api parity batch3 progress

Stage 1 complete - billing mode data model
- Added PricingRule billing_mode and per_request_price fields.
- Added PricingInterval rows for tiered pricing intervals.
- Added UsageLog cost breakdown, requested/upstream model snapshots, and billing_mode snapshots.
- Regenerated Ent code and refreshed Atlas migration hash.

Stage 2 complete - pricing modes and cost breakdown
- Billing EstimatePrice now supports token, per_request, and image billing modes.
- Token mode can use pricing intervals and falls back to flat rule pricing when no interval matches.
- Per-request mode bills zero-token requests from per_request_price.
- Image mode bills image counts from image tiers or per_request_price fallback.
- Gateway pricing results now expose input, output, cache-read, and cache-write cost components.

Stage 3 complete - gateway usage snapshots
- Gateway usage recording persists requested_model, upstream_model, billing_mode, and cost breakdown snapshots.
- Gateway rate multiplier and allowance billing preserve the component breakdown.
- Usage service, memory store, and Ent store apply backward-compatible defaults for older callers.

Stage 4 complete - admin pricing and usage UI
- OpenAPI PricingRule create/update/list schemas now expose billing_mode, per_request_price, and intervals.
- Generated Go OpenAPI types, Copilot spec, and TypeScript SDK are in sync.
- Admin pricing forms support selecting billing mode and editing intervals.
- Usage UI shows total cost with input/output/cache cost breakdown.

Stage 5 complete - non-browser quality gates
- Browser validation was skipped per user instruction.

Evidence:
- make migration-check: ok.
- make ent-generate-check: ok.
- make openapi-codegen-check: ok.
- make openapi-ts-codegen-check: ok.
- make openapi-lint: valid with the existing 3 public-endpoint 4xx warnings.
- cd apps/api && go build ./...: exit 0.
- cd apps/api && go vet ./...: exit 0.
- cd apps/api && go test ./...: exit 0; internal/httpserver passed in 199.018s.
- cd apps/web && npm run lint: exit 0, with existing unused Skeleton warnings only.
- cd apps/web && npx tsc --noEmit: exit 0.
- cd apps/api && go test ./internal/modules/billing/service: ok.
- cd apps/api && go test ./internal/httpserver -run 'TestRecordGatewayUsage(PersistsCostBreakdownAndModelSnapshots|AppliesAccountGroupRateMultiplier)': ok.
- cd apps/api && go test ./internal/persistence/entstore/billing -run TestStorePersistsPricingRules: ok.

Next:
- Batch4: continue with the next goal document. Browser validation remains skipped per user instruction.

SRapi sub2api parity batch4 progress

Stage 1 complete - shared usage window helper
- Added apps/api/internal/pkg/usagewindow for UTC day/week/month starts, rolling 5h/1d/7d starts, and reset/expiry decisions.
- Added unit tests for UTC calendar boundaries, rolling windows, and rolling counter expiry semantics.

Stage 2 complete - materialized usage and API key cost schema
- Added UserSubscription daily/weekly/monthly USD materialized usage counters plus independent window_start fields.
- Added APIKey lifetime cost_quota/cost_used plus 5h/1d/7d cost limits, used counters, and window starts.
- Added migration 000038_materialized_usage_and_key_cost_quotas with down migration and refreshed atlas.sum.
- Regenerated Ent code and OpenAPI Go/TypeScript clients after exposing API key cost fields and subscription materialized usage fields.

Stage 3 complete - billable-cost materialization
- Successful gateway billing now increments active subscription daily/weekly/monthly usage with billable_cost.
- Successful gateway billing now increments API key lifetime and 5h/1d/7d materialized cost counters with billable_cost.
- Subscription and API key reads lazily reset expired windows.
- Added correctness coverage proving repeated successful billing makes materialized subscription/API-key counters match the sum of persisted UsageLog.billable_cost.

Stage 4 complete - bounded hot-path quota reads and key cost rejection
- gatewayUserPeriodUsage now reads monthly tokens via usage.SummarizeUserWindow and subscription monthly cost via MaterializedUsageForUser instead of rescanning all user logs.
- gatewayUserPlatformSpend now uses provider-scoped SummarizeUserWindow queries for daily/rolling-7d/monthly spend instead of rt.usage.ListByUser.
- API key admission rejects requests that would exceed cost_quota or 5h/1d/7d cost windows with rate_limit_exceeded.
- Existing request/rpm/tpm/concurrency limit behavior remains unchanged.

Stage 5 complete - frontend materialized usage and cost quota UI
- API key create/edit dialog now accepts lifetime USD cost quota and 5h/1d/7d cost limits.
- API key edit dialog displays lifetime/5h/1d/7d cost used values.
- Admin subscriptions and user billing subscriptions show daily/weekly/monthly materialized USD usage bars.
- Monthly subscription usage bar uses monthly_cost_quota when present; daily/weekly show materialized values without inventing non-existent daily/weekly plan caps.

Stage 6 complete - non-browser quality gates
- Browser validation was skipped per user instruction.
- Git status remains dirty because batch1-batch4 changes are cumulative and uncommitted in the shared worktree.

Evidence:
- cd apps/api && go test ./internal/pkg/usagewindow ./internal/modules/api_keys/... ./internal/modules/subscriptions/... ./internal/modules/usage/... ./internal/persistence/entstore/apikeys ./internal/persistence/entstore/subscriptions ./internal/persistence/entstore/usage ./internal/httpserver -run 'Test(GatewayEnforcesAPIKeyWindowLimit|GatewayEnforcesAPIKeyCostQuota|GatewayEnforcesAPIKeyCostWindowLimit|GatewayKeyIPAllowed|RecordGatewayUsagePersistsCostBreakdownAndModelSnapshots|RecordGatewayUsageAppliesAccountGroupRateMultiplier|RecordGatewayUsageMaterializedCostMatchesUsageLogBillableSum|MaterializedUsage|StorePersistsPlansAndSubscriptions|StoreCreatesAndLoadsAPIKey|ApplyCostUsage)': exit 0.
- cd apps/api && go test ./internal/architecture ./internal/codequality: exit 0.
- make ent-generate-check: exit 0.
- make migration-check: ok github.com/srapi/srapi/apps/api/internal/platform/db.
- make openapi-lint: valid with the existing 3 public-endpoint 4xx warnings.
- make openapi-codegen-check: exit 0.
- make openapi-ts-codegen-check: exit 0.
- cd apps/api && go build ./...: exit 0.
- cd apps/api && go vet ./...: exit 0.
- cd apps/api && go test ./...: exit 0; internal/httpserver passed in 187.858s.
- cd apps/web && npm run lint: exit 0, with existing unused Skeleton warnings only.
- cd apps/web && npx tsc --noEmit: exit 0.
- Hot-path scan check: rg shows gatewayUserPeriodUsage and gatewayUserPlatformSpend calling SummarizeUserWindow; no rt.usage.ListByUser remains in the gateway period/platform spend paths.

Next:
- Batch5: continue with the next goal document. Browser validation remains skipped per user instruction.

SRapi sub2api parity batch5 progress

Stage 1 complete - quota fetch hardening and forbidden classification
- Provider quota fetches now use a short in-memory cache with failure caching and singleflight coalescing.
- 403 responses are classified into validation_required, policy_violation, or forbidden and preserve validation_url/operator metadata when present.
- Gateway account cooldown remains compatible with legacy auth_failed behavior while accepting the new 403 classes.

Stage 2 complete - Anthropic quota signals
- Anthropic OAuth quota fetch maps five-hour, seven-day, and seven-day Sonnet usage into real QuotaSignal entries.
- Anthropic-compatible gateway responses passively sample unified 5h/7d quota headers into quota signals.
- Quota refresh worker persists structured provider errors and suspends accounts when operator action is required.

Stage 3 complete - provider OAuth presets
- Provider presets include OAuth configuration for Codex, Anthropic, Antigravity, and Gemini-compatible flows.
- Added GET /api/v1/admin/providers/{id}/oauth-config and generated OpenAPI/TypeScript clients.
- The account OAuth dialog can prefill provider OAuth fields from the selected provider.

Stage 4 complete - unified account import
- Standard JSON import and Codex session import now share provider-agnostic import identity keys.
- Standard account import returns the unified created/updated/skipped/failed result shape with item-level warnings.
- The admin import dialog now groups JSON import, Codex session import, and OAuth account authorization behind one entry point.

Stage 5 complete - quota reset and frontend badges
- Added POST /api/v1/admin/accounts/{id}/reset-quota to clear quota error metadata and reactivate blocked accounts.
- Admin accounts list shows quota/validation badges and exposes reset quota when quota error metadata exists.
- HTTP/catalog and provider adapter service code was split into smaller focused files to satisfy architecture/codequality file-size gates.

Stage 6 complete - non-browser quality gates
- Browser validation was skipped per user instruction.
- Git status remains dirty because batch1-batch5 changes are cumulative and uncommitted in the shared worktree.

Evidence:
- cd apps/api && go build ./...: exit 0.
- cd apps/api && go vet ./...: exit 0.
- cd apps/api && go test ./internal/architecture ./internal/codequality -count=1: exit 0.
- cd apps/api && go test ./internal/httpserver -count=1: ok, internal/httpserver passed in 183.948s.
- cd apps/api && go test ./internal/modules/provider_adapters/service ./internal/workers/quota_refresh -count=1: exit 0.
- cd apps/api && go test ./...: exit 0, internal/httpserver passed in 185.819s.
- make openapi-lint: valid with the existing 3 public-endpoint 4xx warnings.
- make openapi-codegen-check: exit 0.
- make openapi-ts-codegen-check: exit 0.
- cd apps/web && npx tsc --noEmit: exit 0.
- cd apps/web && npm run lint: exit 0, with existing unused Skeleton warnings only.
- git diff --check: exit 0.

Next:
- Batch6: continue with available-models, available-channels, usage aggregate cleanup, and ops/account navigation consolidation. Browser validation remains skipped per user instruction.

SRapi sub2api parity batch6 progress

Stage 1 complete - current-user available models API
- Added GET /api/v1/me/available-models to expose static user model visibility, channel status, and effective pricing.
- The handler uses console session auth, subscription allowed_models entitlement, active model/provider/account state, and billing pricing rules.
- Generated Go OpenAPI types, Copilot spec, and TypeScript SDK are in sync.

Stage 2 complete - usage aggregate cleanup
- Replaced separate usage window/model/daily summary DTOs with the shared UsageAggregate contract.
- HTTP response mapping preserves the existing public aggregate_id/aggregate_type OpenAPI shape.
- Gateway and API-key usage summaries now share the same aggregate accumulator and sorting logic.

Stage 3 complete - self-service frontend model availability
- Added the Available channels workspace page backed by /api/v1/me/available-models.
- API key creation now offers current available models as allow-list shortcuts.
- Usage model filtering now comes from the available model list instead of deriving options only from existing logs.

Stage 4 complete - shared usage totals and cost breakdown UI
- Added a shared useUsageTotals hook for user usage and gateway overview totals.
- Usage rows show input/output/cache cost breakdown alongside total cost.

Stage 5 complete - admin navigation consolidation
- Consolidated channel monitor, scheduled tests, strategy replay, and scheduler decisions into /admin/ops tabs.
- Provider account health entry now redirects to a read-only health view under /admin/accounts.
- Removed the old duplicated gateway nav section while keeping gateway resources reachable in admin navigation.

Stage 6 complete - non-browser quality gates
- Browser validation was skipped per user instruction.
- Git status remains dirty because batch1-batch6 changes are cumulative and uncommitted in the shared worktree.

Evidence:
- cd apps/api && go build ./...: exit 0.
- cd apps/api && go vet ./...: exit 0.
- cd apps/api && go test ./...: exit 0; internal/httpserver passed in 249.515s.
- cd apps/api && go test ./internal/modules/usage/service ./internal/httpserver -run 'TestCurrentUserAvailableModelsReturnsChannelStatusAndPricing|TestSummarizeAPIKey|TestAggregate|Test.*Usage' -count=1: exit 0.
- make openapi-lint: valid with the existing 3 public-endpoint 4xx warnings.
- make openapi-codegen-check: exit 0.
- make openapi-ts-codegen-check: exit 0.
- make sdk-ts-typecheck: exit 0.
- cd apps/web && npx tsc --noEmit: exit 0.
- cd apps/web && npm test -- --run tests/unit/messages.test.ts tests/unit/i18n-translate.test.ts: 5 tests passed.
- cd apps/web && npm run lint: exit 0, with 12 existing unused Skeleton warnings outside the files changed in the final cleanup.
- git diff --check: exit 0.

Next:
- Batch6 is complete. Browser validation remains skipped per user instruction.

SRapi sub2api parity batch7 progress

Stage 1 complete - auth allowlist guard
- Added `TestPresetRuntimeAllowlistsOnlyExposeSignableAuthMethods` in the provider preset registry tests.
- The guard asserts every preset `RuntimeClassAllowlist` entry is inside the real signable runtime set; OAuth preset entries must also carry complete provisioning OAuth defaults.
- The guard also asserts OAuth preset entries template a built-in refreshable `upstream_client` (`codex_cli`, `claude_code_cli`, or `antigravity_desktop`), matching Reverse Proxy Runtime refresh settings.
- This would fail on the previous preset state because `service_account_json`, `desktop_client_token`, and `ide_plugin_token` were exposed by default presets but are outside the signable set.

Stage 2 complete - P0 auth method cleanup
- Removed `service_account_json` from the Anthropic and Bedrock provider presets. Later batch11 removed the runtime enum/API/frontend surface entirely.
- Removed unsupported first-party OpenAI and Gemini OAuth runtimes from their presets and OAuth metadata.
- Added provider-adapter `not_supported` handling for manually configured first-party OpenAI/Gemini OAuth accounts.

Stage 3 complete - P1 auth method cleanup
- Merged Antigravity default desktop/IDE token exposure into `oauth_refresh`; the preset now templates `upstream_client=antigravity_desktop` so built-in refresh settings resolve.
- Added `chatgpt-web` provider preset installed as `reverse-proxy-chatgpt-web`, exposing `web_session_cookie` and `custom_reverse_proxy`.
- Removed `desktop_client_token`, `ide_plugin_token`, and `service_account_json` from the frontend account-create fallback runtime list. Later batch11 removed those runtime classes entirely rather than preserving legacy edit compatibility.
- Removed OpenAI OAuth from Quick Setup's static preset list.

Stage 4 complete - regression, docs, and broad gates
- Added `docs/PROVIDER_AUTH_MATRIX.md` as the provider x auth method baseline.
- Updated the sub2api parity README, compatible provider registry spec, and gateway route matrix to match the new auth preset state.
- Added targeted regressions for ChatGPT Web cookie auth, Antigravity OAuth runtime identity, OpenAI/Gemini OAuth not_supported, and provider preset installation auth_methods.
- Anthropic OAuth preset accounts now template `upstream_client=claude_code_cli`; Antigravity templates `upstream_client=antigravity_desktop`.
- `chatgpt-web` is the default web-cookie preset. `reverse-proxy-claude-web` remains roadmap/manual and is not exposed as a default preset.

Final auth class disposition after batch11 cleanup:
- `service_account_json`: removed from OpenAPI, Go, TypeScript SDK, provider presets, and frontend runtime class surfaces.
- `oauth_refresh` / `oauth_device_code`: retained only for presets with provisioning defaults and built-in refresh paths; first-party OpenAI/Gemini presets exclude OAuth and manual OAuth accounts return `not_supported`.
- `desktop_client_token` / `ide_plugin_token`: removed from OpenAPI, Go, TypeScript SDK, provider presets, and frontend runtime class surfaces; Antigravity uses `oauth_refresh`.
- `web_session_cookie`: preset-reachable through `chatgpt-web` installed as `reverse-proxy-chatgpt-web`.
- `custom_reverse_proxy`, `api_key`, `cli_client_token`: unchanged where already wired.

Evidence:
- cd apps/api && go test ./internal/modules/providers/preset ./internal/modules/reverse_proxy/service ./internal/modules/provider_adapters/service ./internal/httpserver -run 'Test(DefaultRegistrySeedsCompatiblePresets|PresetRuntimeAllowlistsOnlyExposeSignableAuthMethods|RuntimeSanitizesHeadersAndInjectsAccountContext|RuntimeInjectsCliClientTokenAndDefaultClientUserAgent|RuntimeDoesNotInjectAPIKeyRuntimeCredentials|AntigravityRefreshUsesClientSecretFormTokenRequest|ClaudeRefreshUsesJSONTokenRequest|AdminAccountAuthMethodAllowlistScopesRuntimeClass|AdminInstallProviderPresetsIsIdempotent|GatewayChatGPTWebPresetAcceptsWebSessionCookie|GatewayAntigravityReverseProxyUsesOAuthRuntimeIdentity|OpenAIPresetOAuthRuntimeIsNotSupported|GeminiPresetOAuthRuntimeIsNotSupported)': exit 0.
- cd apps/api && go build ./... && go vet ./... && go test ./...: exit 0 after guard tightening; internal/httpserver cached, provider preset package passed in 0.006s.
- cd apps/web && npm run lint: exit 0, with 12 existing unused Skeleton warnings only.
- cd apps/web && npx tsc --noEmit: exit 0.
- git diff --check: exit 0.
- Grep/code evidence: `registry.go` OpenAI allowlist only `api_key/custom_reverse_proxy`; Gemini allowlist only `api_key/custom_reverse_proxy`; Anthropic and Bedrock preset allowlists do not include `service_account_json`; Antigravity allowlist is `oauth_refresh/custom_reverse_proxy`; `chatgpt-web` allowlist includes `web_session_cookie/custom_reverse_proxy`; fallback UI runtime list excludes `service_account_json`, `desktop_client_token`, and `ide_plugin_token`.
- Guard red-before explanation: `TestPresetRuntimeAllowlistsOnlyExposeSignableAuthMethods` would fail against the previous preset state because `service_account_json`, `desktop_client_token`, and `ide_plugin_token` are not in the signable set and because OAuth preset entries must carry complete provisioning OAuth defaults plus a built-in refreshable `upstream_client`; after the cleanup it passes.

Next:
- Batch7 behavior is implemented and verified. Per-stage commits and a clean `git status` were not produced because the shared worktree already contains cumulative batch1-batch6 dirty changes plus untracked generated files; committing only batch7 would mix unrelated prior changes in files touched across batches.

SRapi sub2api parity batch8 progress

Stage 1 complete - affiliate invite/rule backend contract and handlers
- Added OpenAPI-first contract for:
  - GET/POST `/api/v1/me/affiliate/invite-codes`.
  - GET/POST `/api/v1/admin/affiliate-rules`.
  - PATCH `/api/v1/admin/affiliate-rules/{id}`.
- Regenerated Go OpenAPI types, Copilot OpenAPI spec, and TypeScript SDK.
- Added affiliate store/service methods for listing current-user invite codes and listing/updating affiliate rules in both memory and Ent stores.
- Extended `/api/v1/me/affiliate` summary with `invite_codes` and `invited_count`.
- Added current-user invite-code handlers with real `CreateInviteCode` writes.
- Added admin affiliate-rule list/create/update handlers with real `CreateRule`/`UpdateRule` writes.
- Added focused HTTP tests proving invite-code create/list/summary exposure and affiliate-rule create/update/list routes work.
- Added optional `invite_code` to public registration and prevalidated/bound it through `ValidateInviteCode` + `BindInvite`.
- Added focused registration test proving a real affiliate relationship is created when registration includes an invite code.
- Added OpenAPI-first affiliate ledger producers:
  - POST `/api/v1/me/affiliate/withdrawals` creates a pending `LedgerTypeWithdraw` request and reserves affiliate balance atomically.
  - POST `/api/v1/admin/affiliates/withdrawals/{id}/approve` settles a pending withdrawal.
  - POST `/api/v1/admin/affiliates/withdrawals/{id}/cancel` cancels a pending withdrawal and releases the reserved balance.
  - POST `/api/v1/admin/affiliates/manual-adjustments` writes `LedgerTypeManualAdjustment`.
- Added affiliate service/store methods for atomic withdrawal reserve, admin approval/cancel, and manual adjustment with idempotent ledger references.
- Removed the orphan `settle` affiliate ledger enum and `settled_amount` summary/API field instead of keeping a zero-producing compatibility surface.
- Added focused service, Ent store, and HTTP tests proving withdrawal pending -> settled/canceled state transitions, balance reservation/release, duplicate approval conflict, summary `WithdrawnAmount`, and manual adjustment real ledger writes.
- Added promo-code backend support for `per_user_limit` and `min_order_amount` in the admin promo contract, API DTOs, generated Go/TS clients, and service validation.
- Enforced promo per-user/minimum-order constraints in memory and Ent admin-control promo preview/finalize paths.
- Added promo usage release support and wired payment cancel/expire transitions to release pending-order promo usage instead of leaking `UsedCount`.
- Marked released promo applications in metadata while preserving usage history.
- Added frontend admin promo form/list fields for `per_user_limit` and `min_order_amount`.
- Added current-user affiliate invite-code SDK/hooks and UI for code generation, copyable code/link, and invited count on `/affiliate`.
- Added current-user affiliate withdrawal request wrapper/hook and withdrawal form on `/affiliate`.
- Added admin affiliate rule SDK/hooks, route/nav entry, form helper, and `/admin/affiliates/rules` page for list/create/edit of active rule fields.
- Added `TestAffiliateInviteRuleAndAccrualFlow`, proving HTTP invite generation -> HTTP registration with `invite_code` -> HTTP admin rule creation -> `AccrueRebate Applied:true` -> accrue ledger write.

Evidence:
- make openapi-codegen-check: exit 0.
- make openapi-ts-codegen-check: exit 0.
- cd apps/api && go build ./... && go vet ./...: exit 0.
- cd apps/api && go test ./internal/modules/affiliate/... ./internal/persistence/entstore/affiliate -count=1: exit 0.
- cd apps/api && go test ./internal/httpserver -run 'Test(AdminAffiliateListsExposeRealDomainRecords|CurrentUserAffiliateSummaryLedgerAndTransfer)' -count=1: exit 0.
- cd apps/api && go test ./internal/httpserver -run 'TestRegister(WithInviteCodeBindsAffiliateRelationship|CreatesSessionWhenEnabled|RejectsDuplicateWithoutLeakingUserState)' -count=1: exit 0.
- cd apps/api && go test ./internal/httpserver -run 'Test(AdminAffiliateListsExposeRealDomainRecords|CurrentUserAffiliateSummaryLedgerAndTransfer|RegisterWithInviteCodeBindsAffiliateRelationship)' -count=1: exit 0.
- cd apps/api && go build ./... && go vet ./...: exit 0.
- make openapi-codegen-check: exit 0.
- make openapi-ts-codegen-check: exit 0.
- make openapi-lint: valid, with the existing 3 public-endpoint 4xx warnings.
- cd apps/api && go test ./internal/modules/admin_control/... ./internal/persistence/entstore/admincontrol -count=1: exit 0.
- cd apps/api && go test ./internal/modules/payments/... ./internal/persistence/entstore/payments -run 'Test(CreateOrderAppliesPromoCodeBeforeCheckout|PaymentOrderStatusMachineRejectsIllegalTransitions|CancelOrderReleasesPromoUsage|ExpirePendingOrdersMarksExpiredOrdersAndWritesAudit|ExpirePendingOrdersReleasesPromoUsage|StoreCreatesDiscountedOrderAndPromoApplicationAtomically|StorePromoLimitsAndRelease)' -count=1: exit 0.
- cd apps/api && go test ./internal/httpserver -run 'Test(AdminAffiliateListsExposeRealDomainRecords|CurrentUserAffiliateSummaryLedgerAndTransfer|AffiliateInviteRuleAndAccrualFlow|RegisterWithInviteCodeBindsAffiliateRelationship)' -count=1: exit 0.
- cd apps/web && npm run typecheck: exit 0.
- cd apps/web && npm run lint: exit 0, with 12 existing unused Skeleton warnings outside this batch8 slice.
- npm run -w apps/web tsc: exit 1 because the root package has no npm workspaces; used `cd apps/web && npm run typecheck` instead.
- cd apps/api && go test ./...: exit 0; internal/httpserver passed in 438.760s.
- make openapi-codegen-check: exit 0 after regenerating with oapi-codegen v2.7.0.
- make openapi-ts-codegen-check: exit 0.
- make openapi-lint: valid with the existing 3 public-endpoint 4xx warnings.
- cd apps/api && go build ./...: exit 0.
- cd apps/api && go vet ./...: exit 0.
- cd apps/api && go test ./internal/modules/affiliate/... ./internal/persistence/entstore/affiliate -count=1: exit 0.
- cd apps/api && go test ./internal/httpserver -run 'Test(CurrentUserAffiliateSummaryLedgerAndTransfer|AdminAffiliateListsExposeRealDomainRecords|AffiliateInviteRuleAndAccrualFlow|RegisterWithInviteCodeBindsAffiliateRelationship)' -count=1: exit 0.
- cd apps/api && go test ./...: exit 0; internal/httpserver passed in 309.016s.
- cd apps/web && npm run typecheck: exit 0.
- cd apps/web && npm run lint: exit 0, with 12 existing unused Skeleton warnings outside this batch8 slice.
- git diff --check: exit 0.

Remaining batch8 work:
- No browser screenshot verification by current user instruction; no browser download or browser validation was performed after this update.
- No compatibility shim remains for orphan affiliate `settle`; the enum/API summary field was removed. `manual_adjustment` and `withdraw` now have real producers.

SRapi sub2api parity batch9 progress

Stage 1 complete - RBAC admin routes and role management
- Added real admin RBAC handlers instead of leaving role/permission UI backed by partial surfaces.
- Added role form/API/query wiring in the frontend admin area.
- Tightened user contract/service/store behavior around roles and permissions.
- Added focused HTTP, module, and store tests for admin role flows.

Stage 2 complete - risk control enforcement path
- Added risk-control admin handler split and tests.
- Wired configured risk controls into gateway/runtime decision points instead of keeping admin settings as inert metadata.
- Updated frontend risk-control form/page/i18n wiring.

Stage 3 complete - content safety configuration and enforcement
- Added content-safety contract/service updates with tests.
- Wired content-safety findings into gateway behavior and audit output.
- Split content-safety gateway helpers from `runtime_gateway_core.go` into `runtime_gateway_content_safety.go` to keep the runtime core file under the 2200-line quality gate.
- Added frontend content-safety form wiring and i18n.

Evidence:
- Focused backend RBAC, risk-control, and content-safety tests passed before batch10 work continued.
- `cd apps/api && go build ./...`: exit 0 after batch10 cleanup.
- `cd apps/api && go vet ./...`: exit 0 after batch10 cleanup.
- `cd apps/api && go test ./internal/architecture ./internal/codequality -count=1`: exit 0 after the gateway helper split.
- `cd apps/web && npm run typecheck`: exit 0 after batch10 frontend changes.
- `cd apps/web && npm run lint`: exit 0, with 12 existing unused Skeleton warnings only.

Next:
- Batch9 behavior is implemented and verified by focused checks. Browser validation was skipped per current user instruction.
- No compatibility shim was kept for fake admin surfaces; settings now either feed enforcement paths or are represented as real CRUD/configuration surfaces.

SRapi sub2api parity batch10 progress

Stage 1 complete - scheduler `priority` compatibility removed
- Replaced scheduler strategy `priority` weight with `quality`.
- Seeded strategies now use `quality`; scheduling score consumption reads `weights["quality"]`.
- Strategy loader rejects the old `priority` key instead of aliasing it.

Stage 2 complete - scheduler strategy CRUD, cache, and lifecycle
- Added typed strategy status/scope contracts, mutation DTOs, and store APIs.
- Added scheduler service CRUD for create/update/activate/deprecate with config hash, lifecycle timestamps, validation, and cache invalidation.
- Scheduler strategy loading now caches active strategies and refreshes on TTL/dirty state instead of hitting persistence for every schedule call.
- Strategy resolution supports global, API key, account group, and user scopes.
- Memory and Ent scheduler stores now list/get/create/update scoped strategies and active scoped registries.

Stage 3 complete - all active strategy scopes load into scheduler
- `ListActiveStrategies` now loads active scoped strategies instead of only global entries.
- Schedule resolution checks request scopes and falls back to global strategies deterministically.
- Scoped strategy tests cover API-key overrides without leaking into non-matching requests.

Stage 4 complete - scheduled-test cron and probe model
- Added real cron parsing for 5-field expressions using local stdlib code, not a new dependency.
- `NextRunAt` uses cron when valid and falls back to interval for invalid expressions.
- Added `probe_model` to scheduled-test contracts, OpenAPI, Ent schema/generated surface, memory/Ent stores, HTTP handlers, TypeScript SDK, and frontend form/list UI.
- The scheduled-test runner now probes `plan.ProbeModel` first, then falls back to account metadata/config.
- Runs with selected accounts but zero probed accounts now record `warning`.
- Added migration `000039_scheduled_test_probe_model.sql` and updated `atlas.sum`.

Stage 5 complete - scheduler and scheduled-test admin frontend
- Replaced the orphan strategy page with live strategy overview, registry, create/update/deprecate/activate controls, simulate, and replay views.
- Added scheduler admin API wrappers, hooks, query keys, SDK exports, and i18n strings.
- Scheduled-test UI now exposes `probe_model`, shows warning status, searches by probe model, and updates cron copy.

Manual generated-code note:
- Ent generation could not be rerun offline because the local Go toolchain is 1.26.2 while the module requires 1.26.3, and the generator path wanted uncached modules. Per user instruction, no download was performed.
- The single new Ent field surface for `scheduled_test_plans.probe_model` was updated manually and validated with build, tests, migration checks, and generated-contract drift checks.

Evidence:
- Scheduler focused tests passed:
  `cd apps/api && go test ./internal/modules/scheduler/service ./internal/persistence/entstore/scheduler ./internal/modules/scheduled_tests/service ./internal/workers/scheduled_test ./internal/httpserver -run 'TestServiceCRUDStrategyFlowsIntoScheduleAndDeprecates|TestScopedStrategyOverridesOnlyMatchingRequestScope|TestStrategyLoaderRejectsPriorityWeightKey|TestStoreListsActiveStrategiesAcrossScopes|TestAdminSchedulerStrategiesReflectActivePersistentStrategy|TestAdminSchedulerStrategyCRUDLifecycle|TestAdminSchedulerSimulationIsDryRun|TestAdminSchedulerReplayUsesPersistedSnapshots|TestNextRunAtUsesCronExpressionWhenValid|TestNextRunAtUsesCronDayOfMonthOrWeekdaySemantics|TestNextRunAtFallsBackToIntervalForInvalidCron|TestRunPlanProbeModelOverridesMetadata|TestRunPlanWithoutProbeModelRecordsWarning|TestAdminScheduledTestPlanProbeModelRoundTrip' -count=1`: exit 0.
- `npm_config_offline=true make openapi-ts-codegen-check OPENAPI_TS='npx --offline @hey-api/openapi-ts@0.97.2'`: exit 0.
- `npm_config_offline=true make sdk-ts-typecheck TSC='npx --offline -p typescript@5.9.3 tsc'`: exit 0.
- Local cached OpenAPI Go generation comparison with `oapi-codegen v2.7.0`: exit 0.
- `make migration-check`: exit 0.
- `make diff-check`: exit 0.
- `cd apps/api && go build ./...`: exit 0.
- `cd apps/api && go vet ./...`: exit 0.
- `cd apps/api && go test ./internal/architecture ./internal/codequality -count=1`: exit 0.
- `cd apps/web && npm run typecheck`: exit 0.
- `cd apps/web && npm run lint`: exit 0, with 12 existing unused Skeleton warnings only.

Not run:
- Browser validation and screenshots were skipped per current user instruction.
- No downloads were performed.
- Broad `go test` over the full httpserver surface was not rerun for batch10; one broad scheduled-tests/httpserver package run hung and was killed, then replaced with focused HTTP/module/store tests plus build/vet/architecture/codequality checks.
- Per-stage commits were not created because the shared worktree already contains cumulative dirty changes from multiple batches.

Next:
- Continue with batch11 from OpenAPI/schema and local-context inspection. Use local cached tooling only.

SRapi sub2api parity batch11 progress

Stage 1 complete - account runtime class cleanup and risk_level contract
- Removed fake runtime classes from OpenAPI/Go/TypeScript/frontend runtime class surfaces: `service_account_json`, `desktop_client_token`, and `ide_plugin_token`.
- Reverse proxy auth injection no longer falls through to Bearer for unknown runtime classes; OAuth paths use the real `oauth_refresh` / `oauth_device_code` classes.
- Added `risk_level` to account Create/Update/import contract paths and frontend account form state.
- Accounts service validates `risk_level` (`normal`, `medium`, `high`) and runtime class on create/update.
- Scheduler risk penalty test now proves high/medium account risk affects scoring.

Stage 2 complete - quota credits persistence and Antigravity quota config
- `QuotaReport` plan/credits are persisted to account metadata and provider-credit snapshots from both manual fetch and the quota refresh worker.
- Added Codex `account_plan.*` fixture coverage for plan/credits mapping.
- Antigravity preset now carries a real `QuotaConfig` with project header expansion and returns `Supported=true` in quota fetch tests.

Stage 3 complete - channel monitor service runner and worker
- Moved monitor target resolution, probe metadata overlay, ProbeAccount execution, and run recording out of HTTP into `channel_monitors/service`.
- Manual `/run` now rejects disabled monitors.
- Added `workers/channel_monitor` and wired it into app start/shutdown; it runs enabled due definitions and records `Trigger=scheduled`.
- Added service and worker tests for disabled skip, interval due selection, scheduled trigger, and probe execution.

Stage 4 complete - TLS profile and risk UI
- Account form now has a dedicated `risk_level` select.
- Account form now has a first-class TLS fingerprint profile switch/dropdown that writes `metadata.tls_profile`.
- Provider account template metadata hints now include `tls_profile`.
- Channel detail TLS panel now aggregates profiles actually bound by accounts under that channel instead of rendering all TLS profiles.

Evidence:
- `rg -n "desktop_client_token|ide_plugin_token|service_account_json" apps/api/internal apps/web/src packages/openapi/openapi.yaml packages/sdk/typescript/src --glob '!**/node_modules/**'`: exit 1, no matches.
- `cd apps/api && go test ./internal/modules/accounts/service ./internal/modules/scheduler/service ./internal/modules/provider_adapters/service ./internal/workers/quota_refresh ./internal/modules/reverse_proxy/service -run 'TestAccountRiskLevelCreateUpdateAndValidation|TestScheduleAppliesConfiguredAccountRiskPenalty|TestFetchAccountQuotaMapsCodexAccountPlanCredits|TestFetchAccountQuotaAntigravityUsesProjectHeaderAndSupportsQuota|TestWorkerPersistsQuotaReportCreditsAndMetadata|TestRuntimeInjectsAntigravityOAuthTokenAndDefaultUserAgent' -count=1`: exit 0.
- `cd apps/api && go test ./internal/modules/providers/preset ./internal/modules/channel_monitors/service ./internal/workers/channel_monitor ./internal/httpserver ./internal/app ./internal/architecture -run 'TestInstallDefaultProvidersFromPresets|TestRunDefinitionRejectsDisabledMonitor|TestRunDueSkipsDisabledAndNotDueAndRecordsScheduled|TestWorkerRunsDueEnabledMonitorWithScheduledTrigger|TestChannelMonitorCRUDAndRun|TestAppBootstrapOnlyImportsBootstrapPackages|TestWorkersOnlyDependOnContractsAndServices' -count=1`: exit 0.
- `npm_config_offline=true npx --offline @hey-api/openapi-ts@0.97.2 -i packages/openapi/openapi.yaml -o packages/sdk/typescript/src -c @hey-api/client-fetch -p @hey-api/typescript @hey-api/sdk --no-log-file`: exit 0.
- Local cached oapi-codegen v2.7.0 generation: exit 0.
- `cd apps/web && npm run typecheck`: exit 0.
- `cd apps/web && npm run lint`: exit 0, with 12 existing unused Skeleton warnings outside this batch11 slice.
- `cd apps/api && go test ./internal/modules/accounts/service ./internal/modules/scheduler/service ./internal/modules/provider_adapters/service ./internal/workers/quota_refresh ./internal/modules/reverse_proxy/service ./internal/modules/providers/preset ./internal/modules/channel_monitors/service ./internal/workers/channel_monitor ./internal/httpserver ./internal/app ./internal/architecture ./internal/codequality -run 'Test(AccountRiskLevelCreateUpdateAndValidation|ScheduleAppliesConfiguredAccountRiskPenalty|FetchAccountQuotaMapsCodexAccountPlanCredits|FetchAccountQuotaAntigravityUsesProjectHeaderAndSupportsQuota|WorkerPersistsQuotaReportCreditsAndMetadata|RuntimeInjectsAntigravityOAuthTokenAndDefaultUserAgent|InstallDefaultProvidersFromPresets|RunDefinitionRejectsDisabledMonitor|RunDueSkipsDisabledAndNotDueAndRecordsScheduled|WorkerRunsDueEnabledMonitorWithScheduledTrigger|ChannelMonitorCRUDAndRun|ProviderAccountAuthMethodsScopedByProvider|AppBootstrapOnlyImportsBootstrapPackages|WorkersOnlyDependOnContractsAndServices|HTTPRuntimeFilesStayPartitioned|GoFilesDoNotExceedLineLimit|FunctionsStaySmall)' -count=1`: exit 0.
- `git diff --check`: exit 0.
- `cd apps/api && go build ./...`: exit 0.
- `cd apps/api && go vet ./...`: exit 0.

Not run:
- Browser validation/screenshots skipped per current user instruction.
- No downloads were performed.

Next:
- Continue into batch12 using the subagent audit notes. Browser validation remains skipped per current instruction.

SRapi sub2api parity batch12-batch19 final progress

Batch12 complete - payment upstream truth
- Extended checkout providers with real `Refund` / `QueryOrder` contracts and implementations for Stripe, WeChat, Alipay, EasyPay, and generic checkout paths.
- `RequestRefund` now transitions through `refunding` and only completes local balance clawback after upstream refund success or confirmed refund webhook state; failures land `refund_failed` with audit evidence instead of pretending local success.
- Added `payment_reconcile` worker for pending order reconciliation through provider `QueryOrder`.
- Added provider instance `fee_rate` and weighted selection, order `fee_amount` / `payable_amount`, typed `payer_openid` / `payer_client_ip`, and admin-readable `PaymentAuditLog` timeline.
- Airwallex remains intentionally not implemented in this slice because the product/provider target is not confirmed; no fake provider surface was added.

Batch13 complete - billing dimensions and daily/weekly quotas
- Pricing rules now support service-tier multipliers, cache-write 5m/1h pricing, image-output pricing, long-context threshold/multiplier, and billing model source selection.
- Billing and gateway pricing consume those dimensions with string + big.Rat fixed-precision money.
- Subscription/user platform quotas now include daily and weekly cost limits and materialized usage surfaces.
- Usage aggregation includes cost breakdowns, and LiteLLM pricing sync is wired as a guarded worker.
- `quality_tier` is consumed as scheduler/quality metadata instead of remaining misleading UI-only copy.

Batch14 complete - end-user auth and platform settings
- Removed fake wechat/dingtalk login exposure rather than keeping non-working buttons.
- OAuth callback now short-circuits already-bound identities into a real session without forcing password rebinding.
- Added passwordless email-code registration/login flow, public site config, `/me/attributes`, self-delete enforcement, console idempotency, captcha settings, backup worker, audit/system-log retention cleanup, and WeChat Pay openid helper path.
- SecuritySecret was not introduced as a second vault; encrypted `Setting.value_ciphertext` remains the single storage path for these configurable secrets.

Batch15 complete - architecture and debt cleanup
- Split hot runtime/provider files and shared repeated helpers instead of adding compatibility layers.
- Current line counts: `runtime_gateway_core.go` 1602, `conversation_protocols.go` 1172, `provider_adapters/service.go` 969, `runtime_api_mapping.go` 1696, `scheduler/service.go` 1648.
- Extracted metadata coercion and quota persistence helpers, cleaned scheduler strategy/pricing legacy key handling, removed dead Codex quota code, and added usage/cost coverage.
- Runtime fake classes `service_account_json`, `desktop_client_token`, and `ide_plugin_token` are removed from OpenAPI, Go, TypeScript SDK, and frontend surfaces.

Batch16 complete - worker concurrency
- Added DB-backed leader gate and `runonceguard` plumbing for periodic workers.
- Wired guarded single-run behavior through balance charger, outbox, idempotency cleanup, payment reconcile, backup, retention, availability rollup, LiteLLM pricing, channel monitor, scheduled test, SLO evaluator, and other periodic workers.
- Preserved existing multi-replica correctness semantics for billing claim and Redis Lua concurrency paths.

Batch17 complete - deploy/readiness/runtime resources
- Redis pool/min-idle/read/write/pool timeouts are configurable and validated.
- `/readyz` now uses explicit dependency pingers; missing pingers report `not_configured`, failed probes report unavailable.
- Release-mode config rejects memory storage and development secrets.
- Docker Compose has restart/resource/healthcheck baselines; k8s API deployment skeleton includes `/readyz` readiness and resources.
- Web build uses Next standalone output and runtime `SRAPI_API_PROXY_TARGET`.

Batch18 complete - gateway hot path performance
- Gateway admission and usage paths use scoped summaries/list methods instead of broad List-then-filter scans.
- Gateway account snapshot and usage side effects are moved through durable outbox processing where correctness requires eventual side effects.
- Pricing and usage summary APIs use scoped store methods; SSE and gateway compatibility behavior remains covered by tests.

Batch19 complete - capacity/observability/read retention
- Added bounded cleanup/retention surfaces for usage and system logs, guarded retention worker, availability rollup worker, and process-local monotonic runtime counters for metrics.
- Added/readied capacity indexes and migration hash through `000041_payment_upstream_truth.sql`.
- Admin ops surfaces can inspect domain outbox and bounded cleanup results without reintroducing full-table hot-path scans.

Final verification on 2026-06-10:
- `cd apps/api && go test ./... -count=1 -timeout=20m`: exit 0.
- `cd apps/api && go vet ./...`: exit 0.
- `cd apps/api && go build ./...`: exit 0.
- `cd apps/api && go test ./internal/architecture ./internal/codequality -count=1`: exit 0.
- `make migration-check`: exit 0.
- `make diff-check`: exit 0.
- `git diff --check`: exit 0.
- `make openapi-codegen-check`: exit 0.
- `npm_config_offline=true make openapi-ts-codegen-check OPENAPI_TS='npx --offline @hey-api/openapi-ts@0.97.2'`: exit 0.
- `npm_config_offline=true make sdk-ts-typecheck TSC='npx --offline -p typescript@5.9.3 tsc'`: exit 0.
- `cd apps/web && npm run typecheck`: exit 0.
- `cd apps/web && npm run lint`: exit 0, with 11 existing unused `Skeleton` warnings.
- `rg -n "desktop_client_token|ide_plugin_token|service_account_json" apps/api/internal apps/web/src packages/openapi/openapi.yaml packages/sdk/typescript/src --glob '!**/node_modules/**'`: exit 1, no matches.

Not run:
- Browser validation/screenshots were skipped per current user instruction.
- No dependency download was intentionally performed. OpenAPI/TypeScript generation used local cached tooling; Go OpenAPI was regenerated with the repository Makefile command after local drift was detected.

SRapi sub2api parity batch20 progress

Batch20 correction scope complete - metrics / retention / indexes / scheduler N+1
- `/metrics` scrape-time usage/scheduler/account scans were removed. Gateway usage and Scheduler decisions now update process-local monotonic metric state on the write path; scrape reads the in-process snapshot plus realtime/reverse-proxy/ops state.
- Provider probe metric collection now reads materialized `AccountAvailabilityRollup` rows through `health_rollups.ListRecent`, not `accounts.List` plus per-account latest snapshots.
- Scheduler active lease gauge now uses `ActiveLeaseCount`; memory, Ent, and Redis scheduler stores implement count-specific paths.
- Retention cleanup now carries `BatchLimit` through config/service/worker/store, deletes by limited ID batches, and reports `Limited`.
- Added `DATA_RETENTION_BATCH_LIMIT` to config, `.env.example`, and Compose.
- Added PostgreSQL migrations and Ent indexes for `usage_logs(provider_id, created_at)` and `account_availability_rollups(bucket_date, provider_id)`.
- Gateway candidate and `/v1/models` account-group filtering now batch account group lookups via `ListGroupIDsByAccounts`.

Batch20 correction scope complete - settings and deployment consistency
- `invitation_rebate_enabled` is consumed by the outbox payment-paid handler; disabled rebate accrual now records audit action `affiliate.rebate.skipped` with reason `invitation_rebate_disabled`.
- `request_shaper_enabled` controls operator payload-transform rules on gateway requests. Internal Anthropic spoof-session compatibility transform remains active.
- Existing consumers for `balance_low_notify_enabled`, `subscription_expiry_notify_enabled`, and `account_quota_notify_enabled` were verified and covered by focused worker tests.
- Kubernetes API deployment now defaults to `replicas: 1`; HPA min replicas is 1 with scale-up comments. Compose documents the `--scale api=N` guard.

Batch20 generated-code and verification evidence
- `make ent-generate`: exit 0.
- `make ent-generate-check`: exit 0.
- `make migration-hash`: exit 0.
- `make migration-check`: exit 0.
- `make openapi-codegen-check`: exit 0.
- `make openapi-ts-codegen-check`: exit 0.
- `cd apps/web && npm run typecheck && npm run lint`: exit 0; lint reported 11 existing unused `Skeleton` warnings.
- `cd apps/api && go test ./... -count=1`: exit 0.
- `cd apps/api && go vet ./...`: exit 0.
- `cd apps/api && go build ./...`: exit 0.
- `cd apps/api && go test ./internal/config ./internal/architecture ./internal/app ./internal/platform/crypto ./internal/platform/db ./internal/platform/logger ./internal/platform/redis ./internal/modules/providers/preset ./internal/persistence/entstore/... ./internal/persistence/redisstore/... ./internal/workers/... ./internal/httpserver -count=1`: exit 0.
- `cd apps/api && go test ./internal/codequality -count=1`: exit 0.
- `make diff-check`: exit 0.
- `make secret-scan`: exit 0.
- Static grep: `rg -n "usage\\.List\\(|ListDecisions\\(|ListLeases\\(|\\.List\\(c\\.ctx\\)|LatestHealthSnapshotByAccount\\(c\\.ctx|accounts\\.List\\(c\\.ctx\\)" apps/api/internal/httpserver/runtime_metrics.go` returns no matches.
- Static grep: all five batch20 settings keys have backend consumers or tests: invitation rebate in outbox, request shaper in gateway payload rules, low-balance in balance_charger, subscription expiry in subscription_expirer, account quota in account_quota_alert.

Next:
- Run browser verification and save screenshots under `specs/plans/parity/evidence/batch20/`.
- Commit batch20 and existing parity work in reviewable domain commits, then push.

SRapi sub2api parity batch20 browser verification

Browser verification complete on 2026-06-10:
- Local test stack: API started with `STORAGE_BACKEND=memory SERVER_MODE=local SERVER_PORT=8080`; Web started with `SRAPI_API_PROXY_TARGET=http://127.0.0.1:8080 PORT=3000 npm run dev`.
- Browser runner: Playwright Chromium, headless, through real `http://localhost:3000` pages and same-origin `/api` proxy.
- Evidence: `specs/plans/parity/evidence/batch20/` contains 19 screenshots plus `browser-smoke-results.json`.
- Result JSON: 46 checks, 0 failed, checked_at `2026-06-10T16:49:32.117Z`.

Browser walk-through coverage:
- Login -> admin dashboard: `01-login-page.png`, `02-admin-dashboard-after-login.png`.
- Affiliate chain surfaces: admin rules, user affiliate withdrawal surface, admin transfer queue, admin rebate ledger: `03-admin-affiliate-rules.png` through `06-admin-affiliate-rebates.png`.
- Scheduler strategy CRUD/simulate surface: registry page and successful simulate result showing current/shadow winner and winner-changed fields: `07-scheduler-strategy.png`, `08-scheduler-strategy-simulated.png`.
- Risk control enforce surface: risk mode/edit controls visible: `09-risk-control.png`.
- Payment order/refund state surface: orders page/status filter visible in empty memory stack: `10-payment-orders-refund.png`.
- Settings switches: invitation rebate, request shaper, low-balance alerts, subscription expiry alerts, and account quota alerts visible in settings tabs: `12-settings-features-switches.png`, `13-settings-gateway-request-shaper.png`, `14-settings-email-notification-switches.png`.
- `/me/attributes` Required/admin registration surface: user attribute Required control and register page loaded: `15-user-attributes-required.png`, `16-user-attributes-required-dialog.png`, `17-register-required-attributes.png`.
- Scheduled-test `probe_model`: scheduled test plan page and create dialog show Probe model field: `18-scheduled-tests-probe-model.png`, `19-scheduled-tests-probe-model-dialog.png`.

Browser-found correction:
- The first settings browser pass showed `balance_low_notify_enabled`, `subscription_expiry_notify_enabled`, and `account_quota_notify_enabled` were consumed by workers but omitted from the default admin-settings GET response because their pointer fields were nil. That made the switches invisible in the real settings UI.
- Fixed `defaultAdminSettings` and `normalizeAdminSettings` to materialize those three notification switches as explicit `true` values, preserving existing worker default behavior while making the UI honest.
- Added `TestAdminSettingsEmailNotificationSwitchesDefaultVisible` to lock the default/update behavior.

Additional verification after browser-found correction:
- `cd apps/api && go test ./internal/modules/admin_control/service -run TestAdminSettingsEmailNotificationSwitchesDefaultVisible -count=1`: exit 0.
- `cd apps/api && go test ./internal/modules/admin_control/service ./internal/httpserver -run 'TestAdminSettingsEmailNotificationSwitchesDefaultVisible|TestAdminControlPlaneV1EndpointsAndAudit|TestMetricsExposeBaselineSRapiSignals|TestGatewaySkipsPayloadRulesWhenRequestShaperDisabled' -count=1`: exit 0.
- `cd apps/web && npm run typecheck`: exit 0.
- `node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('specs/plans/parity/evidence/batch20/browser-smoke-results.json','utf8')); const failed=j.checks.filter(c=>!c.ok); console.log(JSON.stringify({checks:j.checks.length,failed:failed.length,checked_at:j.checked_at}, null, 2)); if(failed.length) process.exit(1);"`: exit 0.

Next:
- Run final gates after the browser-found correction.
- Commit batch20 and existing parity work in reviewable domain commits, then push.

SRapi distinctive program — Wave 1 (frontend 特色速赢) complete on 2026-06-13

Shipped (commits 0f1e742..b94f71e on main):
- docs: specs/plans/goal-distinctive-program.md — 3-way audit synthesis (sub2api delta / first systematic CLIProxyAPI comparison / frontend differentiation), 3 waves.
- W1-1 API key onboarding wizard: create-success panel + row-level 接入指南 with cURL/Python/Node.js/Claude Code snippets (gateway URL + key inlined) and live "test this key" probe against /v1/models. Paste field for existing keys (key stays in browser).
- W1-2 Playground params + persistence: PlaygroundChatRequest gains system/temperature/max_tokens (OpenAPI-first, Go+TS regenerated); composer settings popover; localStorage session restore (images dropped on quota). TestBuildPlaygroundChatBodyParams locks body mapping.
- W1-3 Payment top-up loop: PendingOrderStatus polls GET /api/v1/payment/orders/{id} every 3s while pending, surfaces metadata.checkout_url as 前往支付, confirms + refreshes balance on paid. (Previously the UI never exposed the checkout URL at all.)
- W1-4 Public /key-usage page: login-free key-authenticated GET /v1/usage self-check (balance/spend/model stats/recent requests); linked from sign-in card.
- W1-5 custom_menus rendering: sidebar Links section consumes general.custom_menus (label/name/title + url/href/link tolerated; external links new-tab). Was config-with-zero-consumers.
- W1-6 /scheduler-decisions: verified NOT a dead route — proxy redirects non-admins home, admins land on /admin/ops tab. Left as-is.

Browser verification (memory backend + mock SSE upstream on :8181, CDP Chrome):
- Evidence: specs/plans/parity/evidence/distinctive-wave1/01-04*.png
- Key created via dialog → wizard rendered with real key + selected model; 测试此密钥 → "密钥可用 — 1 个模型". Real gateway stream via curl with the key (mock upstream SSE + usage row, 21 tokens).
- /key-usage end-to-end with that key: 有效 badge, totals, by-model, recent requests.
- Playground: params popover (system+temperature), switched to mock-model, streamed reply, reload → conversation/model/params all restored from localStorage.
- Sidebar renders 链接 section (external 项目文档 + internal 状态页) after setting general.custom_menus via admin settings API.
- Billing verified render-only (no payment method configurable in memory backend; polling logic covered by types + code review).
- Console: zero errors on all visited pages.

Gates at wave end: web typecheck + lint exit 0; apps/api go build + vet + focused playground/quick-setup tests exit 0.

Next: Wave 2 backend smalls — bedrock beta whitelist, refresh singleflight, claude-fable-5 completion; then W2-3/5/6 + Wave 3 大件 (playground对比模式/用量钻取/status页/成本模拟器).
